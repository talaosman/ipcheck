name: Build and Push Docker Image to ACR

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to ACR
        uses: azure/docker-login@v1
        with:
          login-server: talaacr.azurecr.io   # <-- use exact login server from portal
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build Docker image
        run: docker build -t talaacr.azurecr.io/ipcheck:latest .

      - name: Push Docker image
        run: docker push talaacr.azurecr.io/ipcheck:latest
